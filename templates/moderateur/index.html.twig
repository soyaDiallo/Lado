{% extends 'base.html.twig' %}

{% block title %}Moderateur index{% endblock %}

{% block body %}
    <h1>Moderateur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for moderateur in moderateurs %}
            <tr>
                <td>{{ moderateur.id }}</td>
                <td>
                    <a href="{{ path('moderateur_show', {'id': moderateur.id}) }}">show</a>
                    <a href="{{ path('moderateur_edit', {'id': moderateur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('moderateur_new') }}">Create new</a>
{% endblock %}
