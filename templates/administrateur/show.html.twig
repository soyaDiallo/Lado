{% extends 'base.html.twig' %}

{% block title %}Administrateur{% endblock %}

{% block body %}
    <h1>Administrateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ administrateur.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('administrateur_index') }}">back to list</a>

    <a href="{{ path('administrateur_edit', {'id': administrateur.id}) }}">edit</a>

    {{ include('administrateur/_delete_form.html.twig') }}
{% endblock %}
